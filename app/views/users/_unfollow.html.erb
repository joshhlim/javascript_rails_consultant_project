<%= form_for(current_user.user_user_relationships.find_by(followed_id: @user.id),
             html: { method: :delete }, 
             remote: true) do |f| %>
  <%= button_tag "Unfollow", class: "btn btn-large" %>
<% end %>