<%= form_for(current_user.user_user_relationships.build(followed_id: @user.id),
             remote: true) do |f| %>
  <div><%= f.hidden_field :followed_id %></div>
  <%= button_tag "Follow", class: "btn btn-large btn-primary" %>
<% end %>